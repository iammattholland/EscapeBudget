import SwiftUI

/// Facade entry point for design system tokens.
/// Use AppDesign.Theme / AppDesign.Colors for discovery without merging files.
enum AppDesign {
    typealias Theme = AppTheme
    typealias Colors = AppColors
}
